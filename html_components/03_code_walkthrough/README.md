# Code Walkthrough

A 46-second technical walkthrough with runtime-rendered syntax spans, sequential line
reveals, line-specific highlights, and floating callout labels.

Edit `data.json` to change the code, syntax token types, callouts, highlighted line
numbers, takeaway, or timing. No visible content is hardcoded in HTML or JavaScript.
