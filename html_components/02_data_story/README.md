# Data Story

A 42-second metric presentation with a count-up hero stat, progressively drawn SVG
chart, timed annotations, and supporting drivers.

Every value, year label, annotation, driver, and timing lives in `data.json`. The
chart geometry is calculated at runtime from the supplied values.

The included values are explicitly illustrative rather than historical market data.
