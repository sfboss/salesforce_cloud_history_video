# Concept Reveal

A 38-second concept explainer about the cloud release feedback loop.

Edit `data.json` to change the headline, diagram labels, points, takeaway, or timing.
The diagram and all visible text are rendered from that file at runtime.

Double-click `index.html` to autoplay. Add `?record=1` for a one-second blank lead-in
and two-second final freeze.
